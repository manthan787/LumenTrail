{
  "name": "lumentrail",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.12.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "e2e:connectors": "node --import tsx scripts/e2e-connectors.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
