{
  "name": "@lumentrail/gateway",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx --tsconfig tsconfig.dev.json watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'lint not configured'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lumentrail/connectors": "workspace:*",
    "@lumentrail/sdk": "workspace:*",
    "@lumentrail/pipelines": "workspace:*",
    "@fastify/cors": "^8.5.0",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
